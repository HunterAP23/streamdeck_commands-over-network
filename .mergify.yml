pull_request_rules:
  - name: "Auto-Merge Security Updates (dependabot[bot])"
    conditions:
      - author=dependabot[bot]
      - assignee=HunterAP23
    actions:
      merge:
        method: merge

  - name: "Auto-Merge Security Updates (dependabot-preview[bot])"
    conditions:
      - author=dependabot-preview[bot]
      - assignee=HunterAP23
    actions:
      merge:
        method: merge
